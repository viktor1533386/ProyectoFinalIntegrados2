# Copia este archivo como ".env" y completa con tus valores reales.
# ".env" nunca debe subirse al repositorio (ver .gitignore).

APP_URL=http://localhost:81/APF1-INTEGRADOR/public

DB_HOST=localhost
DB_USER=root
DB_PASS=
DB_NAME=bienes_raices
DB_CHARSET=utf8mb4

# Gmail: usar una "contraseña de aplicación", no la contraseña normal de la cuenta.
MAIL_HOST=smtp.gmail.com
MAIL_PORT=587
MAIL_USER=
MAIL_PASS=
MAIL_DESTINO=info@hogarideal.pe

# Clave requerida para acceder a /install.php
INSTALL_SECRET=
